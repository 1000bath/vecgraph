# vecgraph Security

See the [workspace security policy](../SECURITY.md).

Memory records may contain source code, prompts, paths, and personal data. Deployments must define access control, retention, deletion, backup, and encryption policies. Validate anchor paths and never persist API keys or authentication headers as memories.
